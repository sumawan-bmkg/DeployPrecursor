"""
LAWS Shared Core — weights directory marker.

Place model weights here:
  - v8_supcon_best.pth        (PyTorch, ~38 MB)
  - v8_supcon_merged.onnx     (ONNX FP32, ~37.8 MB)

The SharedEncoder auto-detects ONNX first, then PyTorch.
"""
